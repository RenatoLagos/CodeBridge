<section class="hero">
    <div class="hero-content">
        <h1 class="hero-title">
            Transform Low-Code to Real Code
        </h1>
        <p class="hero-description">
            Convert BluePrism, UiPath, and other low-code solutions to Python, Power Automate, and more with just a few clicks.
        </p>
        <div class="hero-buttons">
            <a href="#" class="hero-button">
                Join Waitlist
            </a>    
        </div>
    </div>
</section>

<style>
    .hero {
        max-width: 1440px;
        margin: 0 auto;
        padding: 8px 16px;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 80vh;
        background-color: var(--primary-bg);
    }

    .hero-content {
        text-align: left;
        max-width: 600px;
    }

    .hero-title {
        font-size: 3.5rem;
        font-weight: 800;
        line-height: 1.1;
        margin-bottom: 1.5rem;
        color: var(--text-dark);
    }

    .hero-description {
        font-size: 1.5rem;
        margin-bottom: 2rem;
        color: var(--text-dark);
    }

    .hero-buttons {
        display: flex;
        gap: 1rem;
    }
    
    .hero-button {
        display: flex;
        gap: 1rem;
        background-color: var(--accent-orange);
        color: var(--text-light);
        border: 1px solid var(--border-color);
        box-shadow: 0 2px 4px var(--shadow-color);
        padding: 8px 16px;
        border-radius: 10px;
        text-decoration: none;
        font-weight: 500;
        font-size: 1.25rem;
        transition: background-color 0.2s;
    }  
</style>
